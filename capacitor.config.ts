import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Somya_Dwivedi_Assignment1',
  webDir: 'www'
};

export default config;
